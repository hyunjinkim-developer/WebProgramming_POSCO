let bool = true;
let empty = null;

console.log(`"${typeof 123}" isn't "${"123"}" data type.`);
console.log(
  `type of를 boolean에 사용하면, "${typeof bool}" 결과를 얻을 수 있습니다.`
);
console.log(
  `type of를 array나 null에 사용하면, "${typeof empty}" 결과를 얻을 수 있습니다.`
);
